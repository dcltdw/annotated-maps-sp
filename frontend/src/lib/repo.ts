/**
 * Where this app's source lives, and under what terms.
 *
 * AGPL-3.0 section 13 requires that users interacting with the service over a
 * network are prominently offered the Corresponding Source. `SourceFooter`
 * renders that offer; `AboutButton` repeats it. Both read these constants so
 * the URL cannot drift between them.
 */
export const REPO_URL = "https://github.com/dcltdw/annotated-maps-sp";
export const LICENSE_SPDX = "AGPL-3.0-or-later";
